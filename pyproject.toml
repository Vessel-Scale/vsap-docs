[project]
name = "vsap-docs"
version = "0.1.0"
description = "VSAP Project Documentation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.34",
]

[project.optional-dependencies]
dev = [
    "playwright>=1.58.0",
]
