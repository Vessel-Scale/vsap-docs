[project]
name = "vsap-docs"
version = "0.1.0"
description = "VSAP Project Documentation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.7.6",
]

[project.optional-dependencies]
dev = [
    "playwright>=1.58.0",
]
